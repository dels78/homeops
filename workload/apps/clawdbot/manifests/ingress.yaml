---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clawdbot
  namespace: automation

spec:
  rules:
    - host: clawdbot.delisle.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clawdbot
                port:
                  number: 3000
