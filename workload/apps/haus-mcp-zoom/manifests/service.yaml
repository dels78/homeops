apiVersion: v1
kind: Service
metadata:
  name: haus-mcp-zoom
  namespace: haus-mcp-zoom
  labels:
    app.kubernetes.io/name: haus-mcp-zoom
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: haus-mcp-zoom
    app.kubernetes.io/component: server
