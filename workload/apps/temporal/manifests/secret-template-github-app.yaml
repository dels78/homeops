---
# Template for creating sealed secret
# DO NOT commit actual values - use kubeseal to encrypt
#
# kubeseal --controller-name=sealed-secrets \
#   --controller-namespace=kube-system \
#   --format yaml < secret-template-github-app.yaml > sealedsecret-github-app.yaml
#
apiVersion: v1
kind: Secret
metadata:
  name: github-app
  namespace: temporal
type: Opaque
stringData:
  app-id: "REPLACE_WITH_APP_ID"
  installation-id: "REPLACE_WITH_INSTALLATION_ID"
  private-key: |
    -----BEGIN RSA PRIVATE KEY-----
    REPLACE_WITH_ACTUAL_KEY
    -----END RSA PRIVATE KEY-----
